---
name: save-progress
description: Правила поблочного сохранения результата в output/ через scripts/save_block.py. Гарантирует отсутствие потери данных при сбоях.
---

# Сохранение результата

Единственный способ записи в `output/` — скрипт `scripts/save_block.py`
(запускать из корня проекта). Прямая запись файлов агентом запрещена.

**НЕ РЕДАКТИРУЙ `translates/{ja,en,ru}/*.md` — исходники ТОЛЬКО ДЛЯ ЧТЕНИЯ**
(физически read-only). Результат работы — только `output/` (и
`output/_log/`); merged-файлы — только через `scripts/update_merged.py`.
Проверка целостности translates — посимвольная сверка с git, никогда не
«ремонт» правкой файлов.

## Размер блока

Единица итерации — **смысловой блок** из `translates/` (маркер
`<!-- block: N -->`). Нормализация режет текст так, что один блок — одна
микросцена (реплика + атрибуция, диалоговая пара, короткая наррация; обычно
1–8 абзацев). Обрабатывай и сохраняй **один смысловой блок за итерацию**;
крупный блок сохраняй одной записью (`fix_block.py` заменяет блок целиком).

Для старых томов (абзацная сетка, без маркеров блоков): 10–20 абзацев
за одну итерацию, блок = подряд идущие строки исходных файлов из
`translates/`.

## Команды

Первый блок главы (файл создаётся заново):

```
python scripts/save_block.py --file v14-ch01.md --new --block 1 --text "..."
```

Все последующие блоки (только дописывание):

```
python scripts/save_block.py --file v14-ch01.md --block 2 --text "..."
```

Длинный текст можно передать через stdin:

```
$text = @'
...текст блока...
'@
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new($false)
$text | python scripts/save_block.py --file v14-ch01.md --block 3
```

ВНИМАНИЕ (Windows/PowerShell): без явной установки `$OutputEncoding` в UTF-8
конвейер перекодирует кириллицу в `?`. Надёжнее передавать текст аргументом:
`python scripts/save_block.py ... --text $text`. После каждой записи
обязательно проверяй, что в файле нет знаков «?» вместо букв.

## Контроль записи

Скрипт печатает число символов и размер файла. Правило проверки:
размер файла в байтах должен быть заметно больше числа символов
(кириллица в UTF-8 занимает 2 байта). Если размер ≈ числу символов —
текст испорчен кодировкой: удали блок и перезапиши через `--text`.

Дополнительно `save_block.py` сразу после записи сверяет результат с
исходником: в блочном формате — что номера блоков существуют в
`translates/en` (печатает `(!) блоки … отсутствуют в EN-исходнике`),
в абзацном — позиции абзацев с EN-якорем (профилактика съезда строк,
`(!) позиции: N абзацев ближе к соседней EN-строке…`). Это значит:
разбиение блока не следует EN — поправь блок и разбери план пересадки:

```
python scripts/check_alignment.py --file vXX-chYY.md --propose --report
```

Молча оставлять такое предупреждение нельзя: съезд имеет свойство
накапливаться на всю главу.

## Формат сохраняемого текста

* Первая строка файла (только в блоке 1): заголовок `# <название главы на русском>`.
* Дальше — абзацы, разделённые пустой строкой (как в `translates/`).
* **Блочный формат (новые тома): число блоков результата = числу смысловых
  блоков исходника.** Абзацную структуру русского текста внутри блока
  выбираешь сам по правилам `russian-prose-rules` — можно свободно делить и
  объединять предложения, если смысл JA сохранён; граница блока
  (`<!-- block: N -->`) должна совпадать с исходной. Смысл сверяется по
  блоку JA целиком (EN — вспомогательный).
  *Старые тома (абзацная сетка):* число абзацев результата = числу
  EN-абзацев блока; разбиение следует EN — к нему привязаны merged (ED_RU)
  и иллюстрации; смысл при этом проверяется по JA.
* Внутри одной записи допустим **одиночный перенос строки** (несколько
  визуальных строк), но **не пустая строка**: пустая строка создаёт новую
  запись `ED_RU` и ломает соответствие EN (см. `russian-prose-rules`,
  «Абзацное оформление прямой речи»).
* **RU-колонка — вспомогательное сырьё.** RU — машинный перевод и местами
  семантически мутный; смысловой якорь блока — JA (в помощь EN). В блочном
  формате RU привязан к блокам через EN (логика в `tools/normalize.py`),
  позиционных сдвигов нет. В старом (абзацном) формате RU выровнен по EN
  якорным посиднеечным алгоритмом.
* **Alignment-сигнал ≠ доказательство ошибки.** В блочном формате блоки
  JA/EN/RU выровнены при нормализации, поэтому «съезд» и `ja-shift` не
  возникают; `check_alignment.py` проверяет блок целиком. В старом формате
  сырой `translates/ru` может содержать смещения, а JA — сдвиги (`ja-shift`);
  известное смещение признаётся артефактом только после проверки
  соответствующего JA → RU участка по смыслу. Запрещено объединять/дробить
  абзацы «ради красоты» или ритма и считать каждый JA-shift ошибкой.
* Строки-комментарии `<!-- ??? -->` разрешены для сомнительных мест, НО
  пишутся В КОНЦЕ строки соответствующего абзаца, не отдельным блоком —
  иначе скрипт update_merged.py собьёт соответствие ED_RU ↔ EN-абзацам.
* Неподтверждённые (provisional) решения — новый термин, рабочая форма
  обращения и т. п. — НЕ блокируют сохранение блока: фиксируй их в
  журнале с пометкой `PROVISIONAL` и перечнем мест использования
  (AGENTS.md, «Неопределённость»; UNCERTAINTY ≠ STOP, PROVISIONAL ≠
  CANONICAL).
* Маркеры `<!-- нет пары в JA -->` из исходников в результат НЕ переносить.

## Восстановление после сбоя

Прогресс = маркеры `<!-- block: N -->` в файлах `output/`. Перед началом
работы найди последний блок каждой главы и продолжай со следующего.
Число блоков в файле = число успешно завершённых итераций.

Если маркеров нет, глава ещё не начата: начинай блок 1 (`--new --block 1`)
в этом же запуске после первичной проверки, не дожидаясь отдельного
разрешения пользователя (AGENTS.md, «Правила работы»: первичная проверка —
подготовительный этап; скилл `novel-editor`).

## Обязательное обновление merged

После КАЖДОГО сохранения блока (и после правок через fix_block.py) обновляй
трёхъязычный файл главы:

```
python scripts/update_merged.py --file v14-ch01.md
```

Скрипт переносит отредактированный текст в поле **ED_RU** файла
`translates/_report/merged/v14-*.md`, сопоставляя абзацы с EN-строками
(ED_RU — источник для итоговой склейки перевода с иллюстрациями).
Merged-файлы руками не редактировать — только через этот скрипт.
Если скрипт печатает предупреждение «ED_RU N != EN M» — число абзацев
результата разошлось с исходником: исправь блок.
